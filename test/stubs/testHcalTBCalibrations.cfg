# Configuration file for EventSetupTest_t

process TEST = {

	es_source = PoolDBESSource { VPSet toGet = {
                                     {string record = "HcalPedestalsRcd"
                                      string tag    = "HCAL_PEDESTALS_TB06_V1_RECO_V2"}
                                     }
                   
		    		     bool loadAll = true
                                     string connect = "oracle://cmsr/CMS_ECALHCAL_H2_COND"
			             string timetype = "runnumber" 
                                     untracked uint32 messagelevel = 3
				   }

       source = EmptySource { untracked int32 maxEvents = 10
                                untracked uint32 firstRun = 28232
                                untracked uint32 numberEventsInRun = 0
                              }
 
         module get = EventSetupRecordDataGetter { 
                        VPSet toGet = {
                          { string record = "HcalPedestalsRcd"
                            vstring data = {"HcalPedestals"}
                          } 
                        }
                        untracked bool verbose = true 
         }
 
         module print = AsciiOutputModule { }

         path p = { get }
 
         endpath ep = { print }
 }
